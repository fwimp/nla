# Find the decimal places in a vector of floats

Find the decimal places in a vector of floats

## Usage

``` r
find_dps(v)
```

## Arguments

- v:

  A vector of floats.

## Value

A vector of the number of decimal places required to represent each
float.
